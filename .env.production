# Production-only env. Wird von Next.js beim `next build` gelesen
# (sowohl auf Vercel als auch lokal bei `npm run build && npm start`).
#
# NEXT_PUBLIC_* Variablen sind nicht geheim — sie werden ins Client-Bundle
# eingebettet und sind für jeden Besucher in den DevTools sichtbar.
# Daher gefahrlos committed.

# Google Analytics 4 — Measurement-ID
NEXT_PUBLIC_GA_ID=G-4H357MPD8X
